const x = 50;
const y = 37;

const z = true;

console.log(y > x);

const me = {
  name: "Jermell"
};

// Objects are compared by reference
const someone = {
  name: "Jermell"
};

console.log(me === someone);

console.log(me.name === someone.name);
